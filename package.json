{
  "name": "pck.email.portfolio",
  "version": "1.0.0",
  "description": "Patrick Kilgore's programming portfolio/resume.",
  "author": "Patrick Kilgore <pckilgore@noreply.github.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "build-now": "nuxt generate",
    "start": "serve public",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "local-deploy": "nuxt generate && serve dist",
    "deploy": "nuxt generate && now --public && now alias"
  },
  "dependencies": {
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-loader": "^1.7.1",
    "eslint-plugin-vue": "^4.0.0",
    "husky": "^0.14.3"
  }
}
